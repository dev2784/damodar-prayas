'use client';
import{useCallback,useEffect,useState}from'react';import Link from'next/link';import{useRouter}from'next/navigation';import AdminSidebar from'@/components/admin-sidebar';import{API_BASE_URL,clearAdminSession,getAdminToken,verifyAdminSession}from'@/lib/auth';type T={language?:string;title?:string|null;details?:string|null};type Ad={id:string;status:string;bannerUrl?:string|null;contactName?:string|null;contactPhone?:string|null;location?:string|null;expiresAt?:string|null;translations?:T[]};type Status='PENDING'|'PUBLISHED'|'ARCHIVED';async function api(path:string,o:RequestInit={}){const t=getAdminToken();if(!t)throw new Error('SESSION_EXPIRED');const h:Record<string,string>={authorization:`Bearer ${t}`,accept:'application/json'};if(o.body)h['content-type']='application/json';const r=await fetch(`${API_BASE_URL}${path}`,{...o,headers:h,cache:'no-store'});const d=await r.json().catch(()=>({}));if(r.status===401||r.status===403)throw new Error('SESSION_EXPIRED');if(!r.ok)throw new Error(d?.error||`Request failed (${r.status})`);return d;}const title=(a:Ad)=>a.translations?.find(x=>x.language==='HI')?.title||a.translations?.[0]?.title||'Untitled Advertisement';const details=(a:Ad)=>a.translations?.find(x=>x.language==='HI')?.details||a.translations?.[0]?.details||'';
export default function Page(){const router=useRouter();const[status,setStatus]=useState<Status>('PENDING');const[items,setItems]=useState<Ad[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[busy,setBusy]=useState(false);const load=useCallback(async()=>{setLoading(true);try{if(!await verifyAdminSession()){router.replace('/login');return;}const d=await api(`/admin/posts?status=${status}&category=ADVERTISEMENT&page=1&limit=50`);setItems(d.items||[]);setError(null);}catch(e){if(e instanceof Error&&e.message==='SESSION_EXPIRED'){clearAdminSession();router.replace('/login');return;}setError(e instanceof Error?e.message:'Load failed');}finally{setLoading(false);}},[router,status]);useEffect(()=>{void load();},[load]);async function act(path:string,body?:unknown){setBusy(true);try{await api(path,{method:'POST',body:body?JSON.stringify(body):undefined});await load();}catch(e){setError(e instanceof Error?e.message:'Action failed');}finally{setBusy(false);}}
return <div className="admin-shell"><AdminSidebar/><main className="main moderation-page"><div className="moderation-top"><div><Link href="/" className="back-link">← Dashboard</Link><p className="eyebrow">ADVERTISEMENT MANAGEMENT</p><h1>Advertisements</h1><p className="subtitle">Pending ads के साथ currently live और deactivated ads भी control करें।</p></div><button className="small-btn" onClick={()=>void load()}>Refresh</button></div><div className="moderation-tabs">{(['PENDING','PUBLISHED','ARCHIVED'] as Status[]).map(s=><button key={s} className={status===s?'active':''} onClick={()=>setStatus(s)}>{s==='PUBLISHED'?'Active':s==='ARCHIVED'?'Deactivated':'Pending'}</button>)}</div>{error?<div className="moderation-error">{error}</div>:null}{loading?<div className="empty-state">Ads load हो रहे हैं…</div>:<section className="moderation-list">{items.length===0?<div className="empty-state">इस status में कोई advertisement नहीं है।</div>:items.map(a=><article className="moderation-card" key={a.id}><div className="profile-summary">{a.bannerUrl?<div className="profile-photo"><img src={a.bannerUrl} alt=""/></div>:<div className="profile-photo">AD</div>}<div><h2>{title(a)}</h2><p>{details(a).slice(0,180)}</p><small>{a.location||'Location not provided'}{a.contactPhone?` • ${a.contactPhone}`:''} • {a.status}</small></div></div><div className="moderation-actions">{status==='PENDING'?<><button disabled={busy} className="reject-action" onClick={()=>{const r=prompt('Reject reason:');if(r?.trim())void act(`/admin/posts/${a.id}/reject`,{reason:r.trim()})}}>Reject</button><button disabled={busy} className="approve-action" onClick={()=>confirm('Ad publish करें?')&&void act(`/admin/posts/${a.id}/approve`)}>Approve</button></>:status==='PUBLISHED'?<button disabled={busy} className="danger-action" onClick={()=>confirm('Ad deactivate करें?')&&void act(`/admin/posts/${a.id}/deactivate`)}>Deactivate</button>:<button disabled={busy} className="approve-action" onClick={()=>confirm('Ad फिर से activate करें?')&&void act(`/admin/posts/${a.id}/reactivate`)}>Reactivate</button>}</div></article>)}</section>}</main></div>}
