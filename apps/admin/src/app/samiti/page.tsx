'use client';
import{useCallback,useEffect,useState}from'react';import Link from'next/link';import{useRouter}from'next/navigation';import AdminSidebar from'@/components/admin-sidebar';import{API_BASE_URL,clearAdminSession,getAdminToken,verifyAdminSession}from'@/lib/auth';type T={language:string;name:string;details?:string|null};type M={id:string;name:string;designationHi?:string|null};type C={id:string;status:string;isActive?:boolean;bannerUrl?:string|null;logoUrl?:string|null;city?:string|null;district?:string|null;state?:string|null;phone?:string|null;email?:string|null;translations:T[];members:M[]};type Status='PENDING'|'PUBLISHED'|'ARCHIVED';async function api(path:string,o:RequestInit={}){const t=getAdminToken();if(!t)throw new Error('SESSION_EXPIRED');const h:Record<string,string>={authorization:`Bearer ${t}`,accept:'application/json'};if(o.body)h['content-type']='application/json';const r=await fetch(`${API_BASE_URL}${path}`,{...o,headers:h,cache:'no-store'});const d=await r.json().catch(()=>({}));if(r.status===401||r.status===403)throw new Error('SESSION_EXPIRED');if(!r.ok)throw new Error(d?.error||`Request failed (${r.status})`);return d;}const name=(c:C)=>c.translations?.find(x=>x.language==='HI')?.name||c.translations?.[0]?.name||'Untitled Samiti';const details=(c:C)=>c.translations?.find(x=>x.language==='HI')?.details||c.translations?.[0]?.details||'';
export default function Page(){const router=useRouter();const[status,setStatus]=useState<Status>('PENDING');const[items,setItems]=useState<C[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[busy,setBusy]=useState(false);const load=useCallback(async()=>{setLoading(true);try{if(!await verifyAdminSession()){router.replace('/login');return;}const d=await api(`/admin/committees?status=${status}`);setItems(d.items||[]);setError(null);}catch(e){if(e instanceof Error&&e.message==='SESSION_EXPIRED'){clearAdminSession();router.replace('/login');return;}setError(e instanceof Error?e.message:'Load failed');}finally{setLoading(false);}},[router,status]);useEffect(()=>{void load();},[load]);async function act(path:string,body?:unknown){setBusy(true);try{await api(path,{method:'POST',body:body?JSON.stringify(body):undefined});await load();}catch(e){setError(e instanceof Error?e.message:'Action failed');}finally{setBusy(false);}}
return <div className="admin-shell"><AdminSidebar/><main className="main moderation-page"><div className="moderation-top"><div><Link href="/" className="back-link">← Dashboard</Link><p className="eyebrow">SAMITI MANAGEMENT</p><h1>समिति Management</h1><p className="subtitle">Pending, published और deactivated समितियों को एक जगह manage करें।</p></div><button className="small-btn" onClick={()=>void load()}>Refresh</button></div><div className="moderation-tabs">{(['PENDING','PUBLISHED','ARCHIVED'] as Status[]).map(s=><button key={s} className={status===s?'active':''} onClick={()=>setStatus(s)}>{s==='PUBLISHED'?'Active':s==='ARCHIVED'?'Deactivated':'Pending'}</button>)}</div>{error?<div className="moderation-error">{error}</div>:null}{loading?<div className="empty-state">Samiti data load हो रहा है…</div>:<section className="moderation-list">{items.length===0?<div className="empty-state">इस status में कोई Samiti नहीं है।</div>:items.map(c=><article className="moderation-card" key={c.id}><div className="profile-summary">{c.logoUrl||c.bannerUrl?<div className="profile-photo"><img src={c.logoUrl||c.bannerUrl||''} alt=""/></div>:<div className="profile-photo">{name(c)[0]}</div>}<div><h2>{name(c)}</h2><p>{[c.city,c.district,c.state].filter(Boolean).join(', ')||'Location not provided'}</p><p>{details(c).slice(0,180)}</p><small>{c.members?.length||0} members • {c.status}</small></div></div><div className="moderation-actions">{status==='PENDING'?<><button disabled={busy} className="reject-action" onClick={()=>{const r=prompt('Reject reason:');if(r?.trim())void act(`/admin/committees/${c.id}/reject`,{reason:r.trim()})}}>Reject</button><button disabled={busy} className="approve-action" onClick={()=>confirm('Samiti publish करें?')&&void act(`/admin/committees/${c.id}/approve`)}>Approve</button></>:status==='PUBLISHED'?<button disabled={busy} className="danger-action" onClick={()=>confirm('Samiti public app से deactivate करें?')&&void act(`/admin/committees/${c.id}/deactivate`)}>Deactivate</button>:<button disabled={busy} className="approve-action" onClick={()=>confirm('Samiti फिर से activate करें?')&&void act(`/admin/committees/${c.id}/reactivate`)}>Reactivate</button>}</div></article>)}</section>}</main></div>}
