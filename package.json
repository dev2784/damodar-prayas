{
  "name": "damodar-prayas",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev:api": "pnpm --filter @damodar/api dev",
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "prisma:format": "prisma format --schema=prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.16.2"
  }
}
